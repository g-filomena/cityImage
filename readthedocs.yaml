version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"
  commands:
    - sphinx-build -b html docs/ _build/html

sphinx:
  configuration: docs/conf.py
  
formats:
  - "htmlzip"
  - "pdf"
  - "epub"

python:
  install:
    - requirements: docs/requirements.txt

