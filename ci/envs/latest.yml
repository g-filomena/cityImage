name: test
channels:
  - conda-forge
dependencies:
  - python=3.10  # or a specific compatible version
  - osmnx
  - python-louvain
  - igraph
  - dask
  - numba
  - pyvista
  - tqdm
  # testing
  - pytest
  - pytest-cov
  - codecov
